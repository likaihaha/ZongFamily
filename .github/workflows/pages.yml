name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Prepare Pages artifact
        shell: bash
        run: |
          rm -rf _site
          mkdir -p _site
          cp index.html _site/
          cp .nojekyll _site/
          rsync -a \
            --exclude 'assets/images/future-assets/' \
            --exclude 'assets/images/generated/' \
            --exclude 'assets/images/generated-cn/' \
            game/ _site/game/
          grep -ho "assets/images/future-assets/[^\"' ]*" game/app.js game/index.html | sort -u | while read -r asset; do
            mkdir -p "_site/game/$(dirname "$asset")"
            cp "game/$asset" "_site/game/$asset"
          done

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
